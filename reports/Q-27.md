# Q-27 实验报告：反馈循环上限诊断

- 测试集合：Q-07、Q-08、Q-13、Q-17、Q-27；Q-36因指定SHA缺失已替换。
- 工作流：`flows/q27/q27.workflow`
- 目的：验证 Workflow runtime 对永不满足终止条件的反馈循环进行有界执行，并返回明确的上限诊断，不挂起、不无限重试。

## Hermes

- 运行 ID：`459690b0b23ad5a852ed60acdd22ad4e`
- Hermes 真实执行 `advance` 和 `terminal` 各3次。
- 每轮 TerminalStep 返回严格 JSON Boolean `false`。
- 第3轮后返回：`loop 'terminal' reached max_loop_epochs=3 without terminating`。
- 总耗时约267毫秒。

## Codex

- 运行 ID：`84e0a5361aec1a33f6718b9ea0bc06d9`
- Codex 真实执行 `advance` 和 `terminal` 各3次。
- 第3轮后返回同样的 `max_loop_epochs=3` 诊断。
- 总耗时约451毫秒。

## 修复与验证

- Hermes Program 使用确定性 subprocess 执行，避免把声明的 Program交给交互式 Agent导致挂起。
- Codex复用同一确定性 Program执行路径。
- 三个 epoch均提交后才触发循环上限诊断。
- 未生成错误的最终成功 Artifact，也没有无限循环。

## 结论

通过。Codex 与 Hermes 均能在三轮后稳定报告反馈循环上限，证明 Q-27 的有界循环诊断可被正常调用。
