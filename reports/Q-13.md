# Q-13 实验报告

结论：通过。

测试时间：2026-09-14。环境：`agent-workflow-test312`（Python 3.12），Hermes ACP，真实 `run_flow`/`run_flow_resume`。

工作流先执行一次文档提取 Program，再串行进入两个 Human 断点。运行 ID：`d6a4dfec4ce25289ee8ae34c2ea385f5`。

证据：

- Program `extract` 在首次运行中执行 1 次并产生 `extracted` Artifact。
- 第一次请求问题为“优先评审哪一层？”，选项为 `A shared`、`B api`、`C rawdata`、`D onto`，request_id 为 `65f32ef7f220c3eb1ae93de140f47378`；恢复回答为 `C rawdata`。
- 第二次请求问题为“是否接受这份边界评审作为后续依据？”，选项为 `接受`、`退回补证据`，request_id 为 `da720add48d74a150ed83b14712afc8a`。两个 request_id 不同且顺序正确。
- 第二次恢复回答 `接受` 后才返回最终 `{"second_answer":"接受"}`。恢复日志只派发 `second`，没有重新派发 `extract`。
- 第一次 Human 等待期间没有最终输出，最终 Artifact 只在第二次回答后出现。

期间修复：补齐 `_StepToolRegistry.get()`；为 Hermes 增加 Human ACP 预处理；兼容 Hermes 在 JSON 前添加说明文本的响应。


## Codex复测

运行 ID：99ca7a2f9895a741f7985783d9391122。extract首次执行一次；第一次 Human回答为 C rawdata，第二次回答为 接受，最终返回 second_answer=接受。恢复阶段未重新执行 extract，测试通过。
