# Q-07 实验报告

结论：通过。

测试时间：2026-09-14。真实工作流只包含一个 Agent Step，明确要求调用 
un_flow/
un_flow_resume 启动子 Workflow 并读取 README。

运行 ID：8b65ebf9ae7ebb7c3b1865a936d3781。运行时在 
ested 步骤检测到嵌套 Workflow launcher 请求，抛出 ExecutionPlanError：Workflow launcher tools are unavailable inside Agent Steps。顶层流程在该步骤停止，没有成功输出 Artifact，也没有由顶层流程代替读取 README。

期间修复：Hermes 独立模式补齐 AgentHandle 和工具注册表；增加 Hermes session 超时；在 Agent Step 边界拒绝 
un_flow、
un_flow_resume、low_run 嵌套调用。


## Codex复测

运行 ID：185ce3d9b97c440a2ed4446d1c125349。真实 Codex Workflow runtime 在 nested Agent step 中返回 nested Workflow launcher 被拒绝的 ExecutionPlanError；未启动子 Workflow，测试通过。
