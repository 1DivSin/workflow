# Q-17 实验报告

结论：Program foreach 阶段通过；完整题目尚未通过。

测试时间：2026-09-14。环境：`agent-workflow-test312`（Python 3.12），Hermes 主机适配器，真实 `run_flow`。

工作流 `flows/q17/q17.workflow` 使用固定八个 SHA 作为 `commits` 输入，通过 `foreach_item(inspect, commits) == item` 对每项执行声明的 `git show --format=fuller --stat --name-status`。

运行结果：8 个迭代均完成，输出长度按输入顺序为 `1999, 6588, 5191, 4983, 6273, 2727, 4557, 103419`；运行产物为 `flows/q17/runs/3782e6ef7b2997ae05b83cc17589f66b`。八个 SHA 均存在，固定顺序和 foreach 执行已得到真实证据。

尚缺：每个 commit 的 Agent 独立架构提炼、reduce 保留时间线并选出三个风险转折点，以及 Human 选择后的最小回归集。因此本报告不宣称 Q-17 最终通过。
