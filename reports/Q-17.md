# Q-17 实验报告

结论：通过。

测试时间：2026-09-14。环境：gent-workflow-test312（Python 3.12），Hermes ACP，真实 
un_flow/
un_flow_resume。

运行 ID：420063c9963fae5fa74b5b1594336e25。八个指定 SHA 按固定顺序经 Program foreach 执行真实 git show --format=fuller --stat --name-status，随后八个独立 Agent foreach 分别分析一个 commit；八个 Agent 迭代均完成。

reduce 保留输入时间线并生成三个风险转折点。Human 断点 request_id 为 6227dd61259a5ff6e581f6ccad2a9ba3，提供四个清单选项；选择 接受前三名后 resume 返回 {choice:接受前三名}。恢复阶段未重跑已完成的 foreach 和 reduce。

期间修复：Hermes AgentHandle 和独立工具注册表；Hermes Agent JSON 前缀解析；嵌套 launcher 检测误报；Hermes session 超时。
