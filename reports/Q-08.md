# Q-08 实验报告：Agent TerminalStep 严格布尔与一次 repair

- 测试集合：Q-07、Q-08、Q-13、Q-17、Q-27；Q-36因指定SHA缺失已替换。
- 工作流：`flows/q08/q08.workflow`
- 环境：服务器 Conda 环境 `agent-workflow-test312`（Python 3.12）。

## Hermes

运行 ID：`3fb31de4f716a2f7d88b1f5a004509e7`。

Hermes 首轮将字符串 `"true"` 判定为非法 TerminalStep 输出，复用同一 ACP session 发送一次 repair 后，运行成功生成 `done.md`，最终 state 为 `{"epoch": 1, "probe": "q08"}`。

## Codex

运行 ID：`115fb9b076be7e0e88f90799acb26b53`，最终状态为 `ok`，最终 state 为 `{"epoch": 1, "probe": "q08-codex"}`。

事件日志：

- `flows/q08/runs/codex-events-2686859-0.jsonl`：首轮 `item/agentMessage/delta` 为 `{"done":"true"}`，证明解析值是字符串。
- `flows/q08/runs/codex-events-2686859-1.jsonl`：repair 轮最终包含 `{"done":true}`，证明解析值是严格 JSON Boolean。
- 两轮均收到 `turn/completed`。
- Workflow loop 提交 epoch 1 并正常终止。

## 修复内容

- Hermes Agent TerminalStep 的严格类型错误原先在 workflow runner 归一化层直接抛出，未进入 repair；现已纳入有上限的 repair 循环。
- Hermes repair 复用同一 ACP session。
- Codex adapter 仅提取 `item/agentMessage/delta`，等待 `turn/completed`，并对响应执行严格 JSON 解析和一次 repair。
- Codex adapter 恢复并接入当前分支，Program step 使用确定性执行路径。

## 结论

通过。Codex 与 Hermes 均真实进入 Workflow runtime，首轮错误类型被拒绝，并在一次 repair 后以严格 JSON Boolean `true` 正常终止。
